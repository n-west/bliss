#pragma once

#include <cstdint>
#include <limits>
#include <vector>

namespace bland {

struct ndarray;
struct ndarray_slice;

// ndarray reshape(ndarray x, std::vector<int64_t> new_shape);

// ndarray squeeze(ndarray x, std::vector<int64_t> new_shape);

// ndarray unsqueeze(ndarray x, std::vector<int64_t> new_shape);

// ndarray permute(ndarray x, std::vector<int64_t> new_shape);


} //namespace bland